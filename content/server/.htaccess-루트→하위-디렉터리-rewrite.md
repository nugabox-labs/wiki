+++
title = ".htaccess 루트→하위 디렉터리 rewrite"
date = 2020-12-03T17:23:00Z
updated = 2026-07-21T06:47:00Z
categories = ["SERVER"]
tags = ["WEB"]
toc = true

[extra]
source = "notion"
notion_id = "556de7e0-94c4-4d1d-aded-0ee67ddf3024"
notion_url = "https://app.notion.com/p/htaccess-rewrite-556de7e094c44d1daded0ee67ddf3024"
external_url = "https://qastack.kr/programming/990392/htaccess-rewrite-to-redirect-root-url-to-subdirectory"
+++

루트(`/`)를 하위 디렉터리(예: `/store`)로 보낼 때.

## 외부 리다이렉트 (브라우저 URL 변경)

```javascript
RewriteEngine on
RewriteCond %{REQUEST_URI} ^/$
RewriteRule .* http://www.example.com/store [R=301,L]
```

## 내부(투명) rewrite

```javascript
RewriteEngine on
RewriteCond %{REQUEST_URI} !^/store
RewriteRule ^(.*)$ /store/$1 [NC,L]
```

## 기존 파일은 통과 + 나머지는 subdir

```javascript
RewriteEngine on
RewriteCond %{HTTP_HOST} ^(www.)?example.com$
RewriteCond %{REQUEST_URI} !^/subdir/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /subdir/$1
RewriteRule ^(/)?$ subdir/index.php [L]
```
